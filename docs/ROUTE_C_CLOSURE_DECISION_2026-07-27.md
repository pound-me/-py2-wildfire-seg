# Route C 闭环暂缓：ABL+SAMF 组合待执行

日期：2026-07-27

结论：此前“Route C 已闭环”的结论过早，现正式撤回。按 SAMF 结果归档当日已确认的组合语义，ABL 与 SAMF 均为正向组件，因此 `ABL+SAMF` 两两组合的预注册条件已经为 true，必须补做一次 30 轮筛选；TGM 是否正向只决定是否追加 `ABL+SAMF+TGM` 三元组合。TGM 两项主要增益均为负，因此三元组合不授权，TGM 也不进入 100 轮。测试集继续封存。

## 1. 预注册时间顺序审计

Git提交顺序证明组合规则早于任何TGM结果：

1. `b11da99`：预注册正向组件组合规则；
2. `43b3b7c`：冻结TGM正向数值判据；
3. `3e97573`：实现并完成TGM工程验收；
4. `44a236d`：归档TGM 30轮结果与组合触发判定。

规则、数值判据、工程实现和实验结果的时间顺序没有倒置，但闭环审计错误地把 TGM 正向条件同时施加到了两两组合。用户已明确：当日澄清的真实预注册语义是“两两组合由 ABL 与 SAMF 均正向独立触发；TGM 只控制三元组合”。本次属于纠正预注册执行遗漏，不是依据结果追加新候选或放宽门槛。

## 2. Route C结果总表

| 候选 | 阶段 | mIoU变化 | Fire变化 | RTX 2060 FPS | 决策 |
|---|---|---:|---:|---:|---|
| SAMF | 30轮窗口 | +0.005667 | -0.003271 | 32.32（工程） | 进入100轮 |
| SAMF | 100轮best | +0.001885 | +0.004068 | 34.57 | 未达正式门槛，正向消融 |
| TGM | 30轮窗口 | -0.003078 | -0.006146 | 44.78 | 不进100轮；非正向 |

SAMF主要提高Fire recall但降低precision；TGM同时未改善mIoU与Fire。两者均满足实时硬门槛，但速度合格不能替代精度门槛。

## 3. Amendment 5条件分支纠正

ABL 与 SAMF 的正式结果均呈正向，且分别作用于训练期边界监督和烟概率门控热注入，因此两两组合条件已经满足。TGM 的正向条件只决定其能否加入三元组合。由此：

- `ABL+SAMF` 30轮：已授权且必须执行；采用冻结 SAMF 结构与 ABL 损失，无新增推理结构；
- `ABL+SAMF+TGM` 30轮：不授权，因为 TGM 的 mIoU 与 Fire 增益均为负；
- `ABL+SAMF` 相对 plain Fusion 的门槛为 mIoU `+0.005` 或 Fire IoU `+0.01`，同时不得类别崩溃且 RTX 2060 成对实测不少于 30 FPS；
- 两两组合通过 30 轮门槛才进入一次全新初始化的 100 轮正式实验；未通过时 Route C 方可真正闭环。

此前将两个组合同时判为 false 是执行遗漏，现已纠正；本次纠正不改 seed、数据协议、学习率 horizon、基线、精度门槛或测试集纪律。

## 4. 当前状态与后续边界

- Fusion PIDNet-S继续作为当前最强正式基线；
- SAMF可进入消融和动机分析，但不能称为已成立主方法；
- TGM保留为独立热门控负向消融；
- Route C 状态改为“重新打开：ABL+SAMF 组合待执行”；
- 在组合 30 轮结果产生前不补 3 seed、不解封测试集；
- 若组合通过则按原规则进入 100 轮；若未通过，Route C 才正式闭环；
- 不增加权重扫描、额外结构变体或三元组合。

权威证据见：

- `docs/ROUTE_C_SAMF_100E_FORMAL_RESULT_2026-07-27.md`；
- `docs/ROUTE_C_TGM_30E_SCREENING_RESULT_2026-07-27.md`；
- `docs/EXPERIMENT_RULE_AMENDMENTS.md`；
- 两个运行目录中的metrics、环境、配置哈希、复杂度、延迟和决策JSON。
