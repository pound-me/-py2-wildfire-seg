# Route C SAMF 30 轮筛选结果

日期：2026-07-26

结论：SAMF 通过预注册 30 轮筛选，允许进入 100 轮正式训练。该结论不等同于方法已经成立，仍需以 100 轮正式结果判断。

## 运行与完整性

- 实验：`route_c_pidnet_s_samf/route_c_samf_30e_label_fix_seed200`；
- 配置：`configs/route_c/pidnet_s_fusion_samf_30e_label_fix.yaml`；
- seed：200；GPU：RTX 2060；AMP：开启；
- 训练/验证样本：552/240；
- ImageNet 预训练匹配：301 个张量；
- 完成 30/30 轮，stderr 为空；
- 用时 1127.7 秒，峰值显存 237.8 MB；
- 测试集未使用。

## 第 26–30 轮正式窗口

| 指标 | Fusion 基线 | SAMF | 差值 |
|---|---:|---:|---:|
| mIoU 均值 | 0.786141 | 0.791808 | +0.005667 |
| Fire IoU 均值 | 0.646710 | 0.643439 | -0.003271 |

Fusion 基线第 26–30 轮 Fire 保守噪声带为 `max-min = 0.014251`，不是统计学 sigma。SAMF 的 Fire 差值 -0.003271 位于该噪声带内，因此解释为 Fire 中性，不宣称下降或提升。Smoke 和 Fire 均未发生类别崩溃。

SAMF 满足第一条筛选规则：

```text
mIoU 增益 >= 0.005
且 Fire 增益 >= -Fusion 保守噪声带
```

实际为 `+0.005667 >= +0.005` 且 `-0.003271 >= -0.014251`，故通过。

## 单轮值（只作审计，不用于挑点）

- mIoU：0.789557、0.784989、0.796664、0.791433、0.796394；
- Fire IoU：0.639600、0.642521、0.645630、0.642058、0.647389；
- Smoke IoU：0.829498、0.817997、0.837127、0.828872、0.837173。

单点 best validation mIoU 为 0.7967，不作为 30 轮录用依据。

## 决策

启动唯一已冻结定义的 SAMF 100 轮正式实验：`route_c_samf_100e_label_fix_seed200`。仍从同一 ImageNet 预训练初始化，不从 30 轮 checkpoint 续训，以保持与 Fusion 正式基线训练协议一致。100 轮按验证集 mIoU 保存 best，并使用预注册的正式方法成立门槛判断。熵门不自动启用，TGM 继续冻结到 SAMF 100 轮结论归档后。
